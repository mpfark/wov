<!-- Generated by scripts/project-state.mjs. Edit project-state.json, not this file. -->
# WoV project state

Based on source SHA: `d8366164b8a25529a3e6a587b6d2feb41ba1dfeb`
Last recorded update: 2026-09-22T08:16:04Z

GitHub `origin/main` is authoritative for source. Repository presence does not prove installation, deployment, or frontend publication. A descendant SHA is valid when ancestry is verified. Only Mik manually publishes the frontend.

| Area/artifact | Status | SHA | Evidence | Verification | Note |
|---|---|---|---|---|---|
| origin/main | pushed | d8366164b8a25529a3e6a587b6d2feb41ba1dfeb | repository | direct | Local main fast-forwarded cleanly to origin/main. The previous diagnostic source-quality commit eff0880aa1b68db9f22f7ecd3c96d3461d3ad3a1 and the latest diagnostic/project-state commits are ancestors of the synchronized source. No history was rewritten. |
| src/integrations/supabase/types.ts | installed | b795de05c41ef399788f8c30e2a0bda741de834b | cloud_inspection | direct | Regenerated from the installed schema, not hand-edited. Declarations contain the six new creature reward-channel fields (gold_enabled, gold_min, gold_max, gold_chance, salvage_enabled, item_source) in the creatures Row/Insert/Update shapes, plus combat2_diagnostic_session, combat2_diagnostic_server_event and the five installed diagnostic RPC signatures. |
| Combat2 diagnostic and sync-readiness frontend | ready_for_manual_publish | — | tests | direct | Mik previously reported manual publication of eff0880aa1b68db9f22f7ecd3c96d3461d3ad3a1. Later Stop/Export recovery, healthy-sync readiness and the Holy Shield authoritative stance presentation correction are not published; focused checks and the production build make the current frontend ready for Mik's next manual publication. |
| Mik manually publishes the current frontend, then verifies healthy-sync buttons and the single semantic Holy Shield stance presentation/CP reservation display. | authored | — | handoff | direct | The repository task does not publish the frontend or deploy Edge functions. The next release step must install the unapplied INT+WIS resource migration, redeploy the Combat2 functions identified by dependency containment, and let Mik manually publish the frontend. Authoritative food effects, stance-outside-combat, global resource settlement and ADM-025B remain separate. |
| 20260922100000_int_wis_max_cp_and_combat2_regen.sql | authored | — | tests | direct | Unapplied forward migration reconciles persisted max_cp and sync_character_resources to the canonical effective INT+WIS formula. It preserves absolute current CP, clamps only above the new maximum, grants no free CP, and does not modify authoritative reservation effects. |
| 20260921100000_combat2_bounded_diagnostics.sql | installed | e98635446a7aac8853eb024920479547537a1dfe | cloud_inspection | direct | Applied byte-for-byte (file SHA-256 044fea76b743580be495669a642fa2d6dedfdb396bb84b807b5bb00347e67c64); Cloud ledger entry 20260920234050 recorded and the installed tables, functions, grants and bounds were directly inspected. |
| 20260921110000_combat2_diagnostic_dispatch_integration.sql | installed | e98635446a7aac8853eb024920479547537a1dfe | cloud_inspection | direct | Applied byte-for-byte (file SHA-256 a91116e4c0a634fa5b61807e9fb29ef37742a0a5d4ac495c1eafd64101ab65fe); Cloud ledger entry 20260920234117 recorded and both service-role-only bridge functions were directly inspected. |
| 20260915200000_combat2_authoritative_reward_channels.sql | authored | — | task_boundary | direct | Explicitly excluded from every authorized installation so far; remains unapplied. Its reward-channel columns and node_tick_claim projection patch are the missing half of the proven snapshot_rejected contract defect, but its ADM-025B preflight assertions still abort on live data. |
| 20260921223049_274ba4d5-ba04-41c3-90fd-ab79cd77b566.sql (staged as supabase/pending/20260922000000_combat2_claim_reward_channel_contract.sql) | installed | b795de05c41ef399788f8c30e2a0bda741de834b | cloud_inspection | direct | Applied exactly once. Installed SQL SHA-256 21607d456320fda72477a03dc1c3d572eebfd05ae85a72322d10bc09208e6d14; the staged repository copy was aligned byte-for-byte to the installed ledger artifact and has the identical hash. The originally authored file (SHA-256 2000d01f12df015160594d1141b7243cd634916ea9ef47e3933b73a0ef4fcc9e) aborted fail-closed with 'P0001: claim reward-channel contract patch failed: projection marker not found' and applied nothing, because it targeted public.node_tick_claim(uuid,integer); catalog inspection proved the creature projection marker lives in public.node_tick_claim_without_canary_gate(uuid,integer) at offset 2214, and the two outer wrappers compose that snapshot without re-projecting creature fields. The corrected migration retargets only that function; everything else is unchanged. Installed result verified read-only: creatures.gold_enabled boolean NOT NULL DEFAULT false, gold_min integer NOT NULL DEFAULT 0, gold_max integer NOT NULL DEFAULT 0, gold_chance numeric NOT NULL DEFAULT 0, salvage_enabled boolean NOT NULL DEFAULT false, item_source text NOT NULL DEFAULT 'none'; across all 459 creatures zero null or empty values, zero unsupported item_source values, zero out-of-range gold bounds or chances. Backfill deterministic from existing loot_mode/loot_table authoring: 272 salvage/none, 139 gold/world_pool, 17 gold/none, 10 gold+salvage/none, 10 none/world_pool, 9 none/none, 2 assigned_table. Claim chain unchanged otherwise: node_tick_claim(_node_id uuid, _lease_ms integer DEFAULT 5000), all three functions SECURITY DEFINER, owner postgres, EXECUTE limited to postgres and service_role, fixed search_path, no browser-accessible mutation surface added. No ADM-025B mapping, constraints, triggers, admin RPCs, gameplay-data cleanup or encounter-specific statements. Security linter unchanged at 322 findings. |
| combat2-dispatch-once | deployed | a0516837972791c87356ea12b7fdaa74e5910203 | cloud_inspection | direct | Redeployed successfully with the failure-instrumentation correction (per-node classification and reason are now logged, recording sessions are resolved before the worker runs, and phase/refusal/exception evidence is persisted from a finally path within the 32-event batch bound). Edge packaging and worker mirror parity passed; the dispatcher was not invoked afterwards. No cadence, lease, scheduler, resolver or gameplay change. Redeployed 2026-09-22 with commit-failure diagnostics only: the refusal record now carries a bounded category (pg, pgrst, serde, fetch, unknown), a sanitized five-character code, the commit substage, a closed-set top-level proposal field name and the refused proposal's intent count, plus a fail-closed pre-send serialization guard. No payload contents, SQL, database text, stack traces or personal data are recorded. Canonical worker source and generated Edge mirror at exact parity; the function was not invoked afterwards. No cadence, lease, scheduler, resolver, ability, movement, reward or ADM-025B change. |
| bounded-diagnostics | installed | e98635446a7aac8853eb024920479547537a1dfe | cloud_inspection | direct | Diagnostic schema, RPCs, ACLs, RLS, bounds and dispatcher integration installed and verified; no recording session was started and live latency has not been measured. |
| application-typecheck-defects | ready_for_manual_publish | — | tests | direct | The four reported application TypeScript errors were corrected with type-only/dead-code/test-fixture changes; the intended Stop Recording wording and current reset/RPC contracts are reflected by the focused test. Both root and application TypeScript now pass from a clean non-incremental check. |
| diagnostic-stop-export-recovery | ready_for_manual_publish | 299305fc02fe85bab66f840319b171d100341e87 | tests | direct | Frontend-only correction of the Stop/Export render loop (React error #301): the recording read is now pure, stopped and expired recordings are retained for reload recovery and export, Stop is ref-fenced to one RPC per click and session-fenced against late responses, server-export failure falls back to a local client-only export with a recoverable message, and Start refuses to overwrite an unexported stopped session. Committed bounds (5 minutes, 500 client events, 2000 server events) and admin-only access unchanged. Not published; publication is Mik-only. |
| failed-live-diagnostic-2026-09-21 | unknown | — | cloud_inspection | direct | Session 55ff81c5-3734-498f-a8e5-66cab7a33c86 (started 07:48:16Z, stop committed 07:52:15Z) persisted zero server events because it was scoped to node f6920091, which held no creature or encounter. Read-only inspection shows the scheduler fired every 2s and the dispatcher returned 200, yet no tick was committed in the window (no node_tick_log rows after 07:40Z; the active encounter stayed at tick 20 with unconsumed pending events and an unresolved queued departure). The failing boundary lies between claim acquisition and commit inside the worker, and remains unproven pending an authorized instrumented run. The creature's stale HP came from a pre-existing node_creature row at another node, untouched. |
| dispatcher-failure-instrumentation | deployed | a0516837972791c87356ea12b7fdaa74e5910203 | cloud_inspection | direct | Instrumentation only; no root gameplay fix. Read-only inspection of the preserved encounter 2e7249d4 (node f45a8b21) at 2026-09-21T11:57Z proved the claim boundary succeeds repeatedly (claimed_tick 21 against committed tick 20, claim_expires_at renewed each dispatcher cycle, state_version 29) while no tick commits, so the failing step lies after claim acquisition and at or before commit. The exact step was unprovable because the dispatcher logged only aggregate counts and diagnostic persistence was best-effort after the worker returned. The corrected dispatcher logs per-node classification, reason, stage and code, resolves recording sessions before worker execution, and persists dispatcher selection plus the last reached phase and a sanitized failure classification from a catch/finally path. Worker invocation remains exactly once per candidate; no lock, idempotency, cadence, lease, scheduler or gameplay-rule change. |
| combat2-snapshot-rejected-root-cause | installed | b795de05c41ef399788f8c30e2a0bda741de834b | cloud_inspection | direct | ROOT CAUSE PROVEN, installed SQL contract is behind the deployed worker. The bounded recording showed claim_attempted -> claim_acquired (tick 21) -> snapshot_rejected, 9 cycles, tick frozen at 20. Read-only inspection of the installed public.node_tick_claim chain (node_tick_claim -> node_tick_claim_without_boss_timing -> node_tick_claim_without_canary_gate) shows the creature projection emits id, creature_id, spawn_seq, hp, is_alive, pending_action, tank_fighter_id, engaged, name, level, max_hp, ac, stats, rarity, is_humanoid, is_aggressive, boss_crit_flavors, boss_death_cry, loot_mode, loot_table_id, drop_chance and loot_table only. public.creatures has no gold_enabled, gold_min, gold_max, gold_chance, salvage_enabled or item_source column. The fail-closed decoder in src/shared/combat2/decode.ts (decodeCreature) requires all six, so it rejects every claim at snapshot.creatures[i].gold_enabled (expected boolean), .gold_min/.gold_max/.gold_chance (expected finite number), .salvage_enabled (expected boolean) and .item_source (expected non-empty string); decode never returns, so resolve and commit are unreachable. The fields are added only by the unapplied 20260915200000 reward-channels migration, whose ADM-025B assertions abort. No decoder weakening was performed: nothing may fabricate reward configuration. CORRECTED IN CLOUD on 2026-09-21T22:30:49Z by the contract-only forward migration (ledger 20260921223049), which adds the six columns with a deterministic behaviour-preserving backfill and extends the creature projection in node_tick_claim_without_canary_gate. The installed snapshot now satisfies decodeCreature and malformed snapshots remain fail-closed (regression fixture asserts both). LIVE VERIFICATION PENDING: Combat2 is NOT proven fixed; the world stayed in maintenance and asleep, no worker, dispatcher or gameplay RPC was invoked, and no tick has committed beyond 20. The preserved encounter 2e7249d4, its one present fighter, creature HP, 11 pending events (2 unconsumed), the single queued solo departure 747d045e and all diagnostic evidence were left byte-identical and untouched. |
| ADM-025B | blocked | — | task_boundary | direct | Still pending; legacy unique-item mapping conflicts remain unresolved and no correction is authorized. |
| claim-snapshot-contract | installed | 7adba0dcd7f79731430da4cc02843f1cde2cca96 | cloud_inspection | direct | Installed and now live-verified through decode: the bounded recording c84328c9 shows every automatic attempt reaching claim_acquired, decode_completed and resolve_completed, so the six reward-channel snapshot fields are accepted and snapshot_rejected no longer occurs. Contract unchanged this turn. |
| commit-transport-defect | blocked | — | cloud_inspection | reported/unknown | The historical cause was not proven. Mik reports that the later passive diagnostic session 3be561fc-1bff-4973-a0c1-20bb67b73d6f completed 14 consecutive automatic ticks 27-40 through claim, decode, resolve and commit with zero refusals and zero player actions, so the prior commit transport failure was not reproduced. This does not prove a historical root cause. |
| frontend-sync-flicker | ready_for_manual_publish | — | tests | direct | The last accepted authoritative model now remains action-ready during a healthy transient syncing refresh. Unsafe delivery states, identity mismatches, death, pending departure, invalid targets, insufficient CP and request fencing remain fail-closed. Focused readiness, delivery, intent and rendered-button tests pass. |
| holy-shield-duplicate-presentation | ready_for_manual_publish | — | tests | direct | The authoritative snapshot intentionally contains one reactive Holy Shield effect and one CP-reservation effect. The UI formerly rendered both under the same label. The presentation now groups only the source/target/ability-matched stance pair into one semantic badge while retaining both authoritative records, and the action buttons and CP bar read the authoritative reservation rather than characters.reserved_buffs. |
| in-combat-hp-regeneration | committed | d8366164b8a25529a3e6a587b6d2feb41ba1dfeb | source_audit | direct | The authored rule explicitly pauses passive HP regeneration during combat. Combat2 therefore applies no base CON, equipment, food, milestone or Inn regeneration in a node tick. Explicit authored regeneration effects such as Inspire remain server-authoritative, pulse once per eligible Combat2 tick for living present party members and cap at max HP. No gameplay correction was made. |
| in-combat-cp-regeneration | ready_for_manual_publish | — | tests | direct | Canonical Combat2 resolution now restores WIS-based passive CP on every even encounter-local candidate tick (one four-second opportunity per two committed two-second ticks), before intent costs. Living present non-departing fighters use their own effective WIS and authoritative max CP. Food and Inn are excluded; Inspire remains separate and additive; reservations remain authoritative and passive HP regeneration stays disabled. Edge redeployment is required before this behavior is live. |
| int-wis-max-cp-and-ooc-wis-regen | ready_for_manual_publish | — | tests | direct | Shared, creation, equipment, planning, status and Combat2 calculations use max_cp = 30 + (level-1)*3 + (clamped INT modifier + clamped WIS modifier)*3. Out-of-combat CP regeneration now uses effective WIS while retaining the existing four-second cadence, half-strength browser-local food, milestones, Inn +10, Inspire, rounding and cap behavior. |
| authoritative-food-effects | authored | — | task_boundary | direct | Pending migration and consumption flow. Food remains browser-local and unchanged outside combat, so Combat2 deliberately grants zero food regeneration until the effect is authoritative. |
| global-world-tick-resource-settlement | authored | — | task_boundary | direct | Separate pending architecture topic. It is not required to implement the current authored in-combat regeneration contract and was explicitly excluded from this correction. |
| stance-outside-combat | authored | — | task_boundary | direct | Separate pending feature; explicitly excluded from the sync-flicker correction. |
| diagnostic-frontend-publication | ready_for_manual_publish | — | handoff | direct | Cloud installation and dispatcher deployment remain recorded from prior Lovable evidence. The latest frontend source includes unpublished Stop/Export recovery and sync-readiness corrections; publication remains Mik-only. |
| combat2-commit-transport-root-cause | unknown | — | tests | reported/unknown | Historical root cause remains unknown, but Mik reports the latest passive recording completed ticks 27-40 with zero commit refusals, so the defect was not reproduced in that run. |
| combat2-passive-diagnostic-3be561fc | unknown | — | operator_reported | reported/unknown | Mik reports 14 consecutive automatic ticks 27-40 with claim, decode, resolve and commit completed, zero commit refusals, zero player actions and all 14 snapshots accepted. Each accepted snapshot caused a 60-145 ms live-to-syncing-to-live client transition. Runtime evidence is operator-reported and volatile. |
| combat2-safety-state | unknown | — | cloud_inspection | direct | Observed 2026-09-20T23:45Z: maintenance mode, world asleep, soak off, zero Combat2 cron jobs, zero live claims, zero recording Test Arena runs, zero queued solo or party departures, zero diagnostic sessions, and 3 inert encounter shells. Volatile; re-observe before relying on it. |
| combat2-preserved-stalled-encounter | unknown | — | cloud_inspection | direct | Observed 2026-09-21T11:57Z, read-only and untouched: combat_mode open, encounter 2e7249d4 on node f45a8b21 active at tick 20 with claimed_tick 21, state_version 29, intent_cutoff_seq 99, next_due_at long past, and claim_expires_at renewed within the same second as observation. One present fighter. Diagnostic session 60b1f158 (node and encounter correctly scoped, 11:47:41Z-11:47:54Z) persisted zero server events. Volatile; preserved as diagnostic evidence and not corrected. |
| combat2-live-latency | unknown | — | none | reported/unknown | Still unmeasured. The 2026-09-21 live attempts produced no usable timing. The installed claim contract is now corrected in Cloud, but no live tick has been observed and no recording was authorized, so live Combat2 behaviour remains unproven. |
| combat2-commit-evidence-2026-09-21 | unknown | — | cloud_inspection | direct | Observed read-only from preserved session c84328c9 (encounter 2e7249d4, node f45a8b21): automatic dispatcher attempts reached commit_attempted every cycle; ticks 23, 24 and 25 committed only on the retry following an accepted ability intent, and every no-new-intent retry ended commit_refused/commit_transport_error. Duplicate correlation IDs are retries of the same uncommitted tick. Postgres and edge logs for the window have expired. Volatile; preserved and untouched. |

## Next approved task

Mik manually publishes the current frontend, then verifies healthy-sync buttons and the single semantic Holy Shield stance presentation/CP reservation display.

## Required validation before work

Fetch `origin/main`, verify ancestry from the recorded baseline, run `npm run project-state:check`, and obtain fresh authorized evidence before changing any Cloud or volatile runtime status. Unknown facts remain unknown.
