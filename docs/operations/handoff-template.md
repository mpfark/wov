# WoV handoff

- Starting SHA:
- Synchronized SHA:
- Final local SHA:
- Final remote SHA:
- Recorded-baseline ancestry result:
- Worktree state:
- Files changed:
- Migrations authored:
- Migrations installed (include evidence, or `unknown`):
- Generated Supabase types status:
- Edge deployment status (include evidence, or `unknown`):
- Frontend status: `ready for Mik's manual publication` / `manually published by Mik (operator evidence)` / `unknown`
- Tests and build:
- Cloud operations:
- Gameplay operations:
- Deviations:
- Blockers:
- Next safe action:

“Published” must never mean pushed to Git. Use `pushed` for Git and reserve `manually published` for Mik's confirmed frontend action.
